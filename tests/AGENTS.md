# tests/

- Prefer deterministic tests using temp directories, fixtures, and injected fakes.
- Keep helper coverage separate from extension-flow coverage when possible.
- Make sure tests can run without relying on the Pi UI or network services.
